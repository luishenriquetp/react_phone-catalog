import React from 'react';
import StyledComponentTemplate from './StyledComponentTemplate.ts';

/* interface ComponentTemplateProps {
  // Define here props of this Component
} */

function ComponentTemplate(/* props: ComponentTemplateProps */): React.ReactNode {
  /* const {
    // someProps
  } = props; */

  return <StyledComponentTemplate>ComponentTemplate Component!</StyledComponentTemplate>;
}

export default ComponentTemplate;
